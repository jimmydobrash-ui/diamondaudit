# app.diamondaudit.io is a fully authenticated application — every route is
# behind a login and renders an empty shell to crawlers. Keep it out of the
# index. (Not a security control; that's Supabase RLS + ProtectedRoute.)
User-agent: *
Disallow: /
