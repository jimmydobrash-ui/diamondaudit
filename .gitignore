# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Vercel CLI local project link (org/project IDs; this repo deploys via git
# integration, not CLI, so linkage is machine-local tooling state only)
.vercel

# Environment
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude-flow/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Database backups (contain PII — never commit)
/backups/
*.dump
